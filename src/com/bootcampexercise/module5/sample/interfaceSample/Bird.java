package com.bootcampexercise.module5.sample.interfaceSample;

// Bird class implements Moveable interface
public class Bird implements Moveable {

    // implements move() method from the Move able interface
    public void move(){
        System.out.println("Birds can fly");
    }
}
